import 'package:flutter/material.dart';

class EdoneAiScreeen extends StatelessWidget {
  const EdoneAiScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Edone AI Screen")));
  }
}
